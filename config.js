// Number of leading zero bits for proof of work
const DIFFICULTY = 4;

// Rate at which blocks are mined in milliseconds
const MINE_RATE = 5000;

// For simulation purposes, users start with an existing balance
const INITIAL_BALANCE = 500;

// 
const MINING_REWARD = 10;
module.exports = { DIFFICULTY, MINE_RATE, INITIAL_BALANCE, MINING_REWARD };